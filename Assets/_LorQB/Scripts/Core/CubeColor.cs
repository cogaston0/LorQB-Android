namespace LorQB.Core
{
    /// <summary>
    /// Identifies each of the four cubes in Level 1.
    /// Order matches the default chain arrangement: Blue → Red → Green → Yellow.
    /// </summary>
    public enum CubeColor
    {
        Blue   = 0,
        Red    = 1,
        Green  = 2,
        Yellow = 3
    }
}
